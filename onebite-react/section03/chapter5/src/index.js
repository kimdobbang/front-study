import mul, { add, sub } from "./math.js";

import randomcolor from "randomcolor";

const color = randomcolor();
console.log(color);

// console.log(add(1, 2));
// console.log(sub(1, 2));
// console.log(mul(2, 3));

// node_modules, pakage-lock.json -> 지워져도 package.json 기준으로 npm i로 재설치가git에 안올림
